import { Locale } from "date-fns";
import { Days } from "./use-weekview";
import { Event } from "./weekview";
export default function EventGrid({ days, events, weekStartsOn, locale, minuteStep, rowHeight, onEventClick, }: {
    days: Days;
    events?: Event[];
    weekStartsOn: Day;
    locale?: Locale;
    minuteStep: number;
    rowHeight: number;
    onEventClick?: (event: Event) => void;
}): import("react/jsx-runtime").JSX.Element;
