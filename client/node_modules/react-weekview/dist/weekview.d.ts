import { Cell } from "./use-weekview";
export type Event = {
    id: string;
    title: string;
    startDate: Date;
    endDate: Date;
};
export default function WeekView({ initialDate, minuteStep, weekStartsOn, locale, rowHeight, disabledCell, disabledDay, disabledWeek, events, onCellClick, onEventClick, }: {
    initialDate?: Date;
    minuteStep?: number;
    weekStartsOn?: Day;
    locale?: Locale;
    rowHeight?: number;
    disabledCell?: (date: Date) => boolean;
    disabledDay?: (date: Date) => boolean;
    disabledWeek?: (startDayOfWeek: Date) => boolean;
    events?: Event[];
    onCellClick?: (cell: Cell) => void;
    onEventClick?: (event: Event) => void;
}): import("react/jsx-runtime").JSX.Element;
