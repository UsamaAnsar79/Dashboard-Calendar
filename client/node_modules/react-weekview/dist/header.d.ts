import { ReactNode } from "react";
export default function Header({ title, showTodayButton, todayButton, onToday, showPrevButton, prevButton, onPrev, showNextButton, nextButton, onNext, }: {
    title: ReactNode;
    showTodayButton?: boolean;
    todayButton?: ({ onToday }: {
        onToday?: () => void;
    }) => ReactNode;
    onToday?: () => void;
    showPrevButton?: boolean;
    prevButton?: ({ onPrev }: {
        onPrev?: () => void;
    }) => ReactNode;
    onPrev?: () => void;
    showNextButton?: boolean;
    nextButton?: ({ onNext }: {
        onNext?: () => void;
    }) => ReactNode;
    onNext?: () => void;
}): import("react/jsx-runtime").JSX.Element;
